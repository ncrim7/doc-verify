# === Core pipeline ===
python-dotenv>=1.0.0
openai>=1.0.0
PyMuPDF>=1.23.0          # PDF -> image (fitz)
Pillow>=10.0.0           # image handling in fallbacks

# === Synthetic dataset generation (scripts/generate_dataset.py) ===
faker>=20.0.0
reportlab>=4.0.0

# === Optional: alternative LLM provider ===
# Disabled in src/config.py by default (see docs/adr/0001-extraction-model.md).
# Uncomment the provider block in config.py to use it.
groq>=0.4.0

# === Optional: LLM observability ===
# Auto-disabled when LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY are unset.
langfuse>=2.0.0

# === Dev / test ===
pytest>=8.0.0
pytest-cov>=5.0.0

# --- removed vs graduation project ---
# google-generativeai : Gemini never used (quota-blocked during thesis)
# streamlit           : dashboard lives in a separate repo
# python-telegram-bot : bot lives in a separate repo
# pydantic            : not imported by core (re-add if a schema module needs it)
